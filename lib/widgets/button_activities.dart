import 'package:flutter/material.dart';

class ButtonActivities extends StatelessWidget {
  const ButtonActivities({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
